## Packages
date-fns | Date formatting and manipulation for social feed timestamps
lucide-react | Beautiful, consistent iconography
react-hook-form | Form state management
@hookform/resolvers | Zod validation integration for forms
zod | Schema validation

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}

Replit Auth is already configured in the backend and the `useAuth` hook is pre-existing.
